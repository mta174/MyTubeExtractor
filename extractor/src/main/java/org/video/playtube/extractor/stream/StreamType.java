package org.video.playtube.extractor.stream;

public enum StreamType {
    NONE,   // placeholder to check if streams type was checked or not
    VIDEO_STREAM,
    AUDIO_STREAM,
    LIVE_STREAM,
    AUDIO_LIVE_STREAM,
    FILE
}
